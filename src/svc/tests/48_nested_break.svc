
func main() {

   a = 0;
   while (a < 4) {
      print a++, "<<- a \n";
      if (a > 2) break;

      b = 60;
      for (i = 0; i < 10; i++) {
         while (b < 67) {
          print b++, " <<- b \n";
          if ( b >= 66) { return 1; 
          } else break; 
         }
         print b, " f: \n";
      }
      print "e: ";
   }
   print "\n";

   return 0;
}

/* vim: set expandtab ts=4 sw=3 sts=3 tw=80 :*/

main();
